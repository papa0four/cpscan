#!/bin/bash
set -euo pipefail

# =============================================================================
# cpscan uninstall script (macOS)
#
# This script is a placeholder. macOS uninstall support is planned
# for a future release.
#
# For now, macOS users can uninstall manually:
#   sudo rm -f /usr/local/bin/cpscan
# =============================================================================

echo "[-] macOS uninstall script is not yet implemented."
echo "    To uninstall cpscan on macOS, remove the binary manually:"
echo ""
echo "    sudo rm -f /usr/local/bin/cpscan"
echo ""
echo "    Visit https://github.com/papa0four/cpscan for updates."
exit 1