#!/bin/bash
set -euo pipefail

# =============================================================================
# orkowatch uninstall script (macOS)
#
# This script is a placeholder. macOS uninstall support is planned
# for a future release.
#
# For now, macOS users can uninstall manually:
#   sudo rm -f /usr/local/bin/orkowatch
# =============================================================================

echo "[-] macOS uninstall script is not yet implemented."
echo "    To uninstall orkowatch on macOS, remove the binary manually:"
echo ""
echo "    sudo rm -f /usr/local/bin/owatch"
echo ""
echo "    Visit https://github.com/papa0four/orkowatch for updates."
exit 1